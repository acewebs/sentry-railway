{
  "//": "Generate per deployment; do NOT commit the real credentials.json (gitignored).",
  "//generate": "docker compose run --rm relay credentials generate --stdout > relay/credentials.json",
  "//whitelist": "After generating, copy the public_key into SENTRY_RELAY_WHITELIST_PK in sentry/sentry.conf.py so the internal relay is trusted (see docs/RAILWAY-DEPLOY-NOTES.md).",
  "secret_key": "REPLACE_WITH_GENERATED_SECRET_KEY",
  "public_key": "REPLACE_WITH_GENERATED_PUBLIC_KEY",
  "id": "REPLACE_WITH_GENERATED_UUID"
}
