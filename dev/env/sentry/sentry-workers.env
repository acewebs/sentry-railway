# sentry-workers — template variables
# Four honcho processes: ingest-events, post-process-forwarder, taskworker, taskworker-scheduler.

SENTRY_CONF=/etc/sentry
# Directory holding sentry.conf.py / config.yml inside the image.
SENTRY_EVENT_RETENTION_DAYS=90
# Days to retain events before the cleanup job deletes them.
SENTRY_SYSTEM_SECRET_KEY=${{ sentry-web.SENTRY_SYSTEM_SECRET_KEY }}
# Django SECRET_KEY; references the value generated on sentry-web (must match).
SNUBA=http://${{ snuba-api.RAILWAY_PRIVATE_DOMAIN }}:1218
# URL of the Snuba API service (ClickHouse query layer).
RAILWAY_DOCKERFILE_PATH=railway/sentry-workers/Dockerfile
# Path to this service's Dockerfile in the repo.
