# gateway (nginx) — template variables
# Single public service; routing baked into nginx.conf (targets sentry-relay:3000, sentry-web:9000).

RAILWAY_DOCKERFILE_PATH=railway/nginx/Dockerfile
# Path to this service's Dockerfile in the repo.
