# redis — template variables
# Stock redis image, no configuration. Runs on the default port 6379, no auth on the
# private network, no volume (buffers/queues only). Peers reach it via
# ${{ redis.RAILWAY_PRIVATE_DOMAIN }}. No service variables to set.
