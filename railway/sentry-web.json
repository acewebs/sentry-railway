{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "railway/sentry/Dockerfile"
  },
  "deploy": {
    "preDeployCommand": "bash /etc/sentry/bootstrap.sh",
    "startCommand": "run web"
  }
}
