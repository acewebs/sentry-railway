{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "railway/relay/Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/api/relay/healthcheck/live/",
    "healthcheckTimeout": 300
  }
}
