# Secrets — generated per deployment, never commit
relay/credentials.json
.env
.env.custom
.env.railway
*.pem

# GeoIP databases (large, licensed; download separately if needed)
geoip/*.mmdb
geoip/GeoLite2-*.tar.gz

# Certificates
certificates/*.crt
certificates/*.key

# Local build / runtime artifacts
node_modules/
.DS_Store
*.log

# Railway/editor local state
.railway/
.idea/

# local install artifacts
sentry_install_log-*.txt

# internal planning / announcement drafts (not for the public template repo)
dev/docs/
